import 'package:flutter/cupertino.dart';

class MyHabitsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('My Habits Page', style: TextStyle(fontSize: 24)),
    );
  }
}